{
  "name": "digitatex-site",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio de Digitatex — HTML autocontenido, sin build.",
  "scripts": {
    "start": "serve --listen ${PORT:-3000} --no-clipboard ."
  },
  "dependencies": {
    "serve": "^14.2.6"
  },
  "engines": {
    "node": ">=18"
  }
}
